import { makeStyles } from "@mui/styles";

export const indexStyles = makeStyles((ctx) => {
  return {
    wrapper: {
      // border: `1px solid ${ctx.palette.primary.main}`,
      // padding: "15px",
      display: "flex",
      position: "relative",
      height: "100vh",
    },
  };
});
