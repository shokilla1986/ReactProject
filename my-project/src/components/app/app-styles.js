import { makeStyles } from "@mui/styles";

export const appStyles = makeStyles((ctx) => {
  return {
    wrapper: {
      display: "flex",
      position: "relative",
      height: "90%",
    },
  };
});
