import { makeStyles } from "@mui/styles";

export const headerStyles = makeStyles(() => {
  return {
    header: {
      width: "100%",
      height: "50px",
      backgroundColor: "grey",
    },
  };
});
