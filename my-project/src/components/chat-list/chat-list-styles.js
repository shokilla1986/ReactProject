import { makeStyles } from "@mui/styles";

export const ChatListStyles = makeStyles(() => {
  return {
    wrapper: {
      width: "30%",
      height: "100%",
      backgroundColor: "whitesmoke",
    },

    a: {
      display: "block",
      textDecoration: "none",
    },
  };
});
