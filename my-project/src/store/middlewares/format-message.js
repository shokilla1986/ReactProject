// import { FORMAT_MESSAGE } from "../messages/types";
// import { formatMessage } from "../messages";

// export const formatUserMessage = (store) => (next) => (action) => {
//   if (action.type === FORMAT_MESSAGE) {
//     store.dispatch(
//       formatMessage(
//         { message: action.payload.messageValue },
//         action.payload.roomId
//       )
//     );
//   }

//   return next(action);
// };
